pipeline {
    agent any
    stages {
        stage ('Initialize') {
            steps {
                echo 'Initialize stage'
            }            
        }

        stage ('Build') {
            steps{
                echo 'Build stage'
            }
        }

        stage ('Deploy') {
            steps {
                echo 'Deploy stage'
            }
        }
    }
}
